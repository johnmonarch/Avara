# Coolify Notes

- Use [`infra/docker-compose.yml`](/Users/monarch/Documents/Avara/infra/docker-compose.yml) as the stack entrypoint.
- Set the values from [`infra/.env.example`](/Users/monarch/Documents/Avara/infra/.env.example) in Coolify environment variables.
- Scale `game-server` independently once the authoritative worker moves beyond the MVP stub.
